Question 2.1: 
     Compare kern/mpentry.S side by side with boot/boot.S. Bearing in mind that kern/mpentry.S is compiled and linked to run above KERNBASE just like everything else in the kernel, what is the purpose of macro MPBOOTPHYS? Why is it necessary in kern/mpentry.S but not in boot/boot.S? In other words, what could go wrong if it were omitted in kern/mpentry.S? Hint: recall the differences between the link address and the load address that we have discussed in Lab 1.
          The purpose of the macro MPBOOTPHYS is to convert the virtual address to a physical address. 
          This is necessary in kern/mpentry.S because it is run above KERNBASE, which means that it is using virtual addresses. 
          If it were omitted, the code would try to jump to a virtual address that is not mapped to a physical address, leading to a page fault.
Question 5.1:
     It seems that using the big kernel lock guarantees that only one CPU can run the kernel code at a time. Why do we still need separate kernel stacks for each CPU? Describe a scenario in which using a shared kernel stack will go wrong, even with the protection of the big kernel lock.
          The big kernel lock only protects the kernel code from being executed by multiple CPUs at the same time, but it does not protect the kernel stack from being accessed by multiple CPUs.
          If two CPUs were to share a kernel stack, one CPU could be executing code that modifies the stack while another CPU is trying to access the same stack, leading to data corruption and unpredictable behavior.
Question 6.1:
     In your implementation of env_run() you should have called lcr3(). Before and after the call to lcr3(), your code makes references (at least it should) to the variable e, the argument to env_run. Upon loading the %cr3 register, the addressing context used by the MMU is instantly changed. But a virtual address (namely e) has meaning relative to a given address context–the address context specifies the physical address to which the virtual address maps. Why can the pointer e be dereferenced both before and after the addressing switch?
          The pointer e can be dereferenced both before and after the addressing switch because the virtual address space is the same for all processes. 
          The kernel uses a fixed mapping for the kernel address space, so the virtual address of e will always map to the same physical address regardless of the current addressing context.
Question 6.2:
     Whenever the kernel switches from one environment to another, it must ensure the old environment’s registers are saved so they can be restored properly later. Why? Where does this happen?
          The kernel must ensure that the old environment's registers are saved so that the state of the old environment can be restored when it is scheduled to run again. 
          This happens in the env_run() function, where the registers of the old environment are saved on its kernel stack before switching to the new environment.
